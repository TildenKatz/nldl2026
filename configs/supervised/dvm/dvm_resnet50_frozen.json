{
  "experiment_type": "supervised",
  "experiment_name": "dvm_resnet50_frozen",
  "data": {
    "type": "dvm"
  },
  "model": {
    "encoder": {
      "type": "resnet",
      "variant": "resnet50"
    },
    "head": {
      "hidden_dim": null,
      "bounded_outputs": false
    },
    "freeze_encoder": true
  },
  "training": {
    "batch_size": 128,
    "max_epochs": 100,
    "optimizer": {
      "type": "adam",
      "lr": 0.001,
      "weight_decay": 0.0
    },
    "scheduler": null,
    "early_stopping_patience": 15,
    "early_stopping_min_delta": 0.0001,
    "seed": 42
  },
  "loss": {
    "type": "cross_entropy"
  }
}
