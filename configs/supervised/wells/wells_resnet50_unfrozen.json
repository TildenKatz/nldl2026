{
  "experiment_type": "supervised",
  "experiment_name": "wells_resnet50_unfrozen",
  "data": {
    "type": "wells"
  },
  "model": {
    "encoder": {
      "type": "resnet",
      "variant": "resnet50"
    },
    "head": {
      "hidden_dim": null,
      "bounded_outputs": true
    },
    "freeze_encoder": false
  },
  "training": {
    "batch_size": 64,
    "max_epochs": 100,
    "optimizer": {
      "type": "adam",
      "lr": 0.0001,
      "weight_decay": 1.5e-6
    },
    "scheduler": {
      "type": "cosine",
      "warmup_epochs": 5,
      "warmup_start_factor": 0.1
    },
    "early_stopping_patience": 15,
    "early_stopping_min_delta": 0.0001,
    "seed": 42
  },
  "loss": {
    "type": "mse"
  }
}
